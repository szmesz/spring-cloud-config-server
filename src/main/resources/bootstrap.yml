spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/szmesz/config-repo
        native:
          searchLocations: file:///${user.home}/Desktop/config
